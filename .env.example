# API Configuration
# The base URL for the snailbus API backend
# In production, this should be set to your API server URL
VITE_API_BASE_URL=http://localhost:8080

# Application Configuration
# Application name (used in titles, headers, etc.)
VITE_APP_NAME=Slime UI

# Application version
# This will be read from package.json if not set
VITE_APP_VERSION=0.1.0
